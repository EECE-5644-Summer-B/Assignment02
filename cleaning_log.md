# Cleaning Log

As shown in the main `notebook.ipynb` document, no additional cleaning of the given `telco.csv` data was required, as all `SERVICES` fields were completely filled, with no `NaN` or `NULL` values found. Given we were interested in the latent effects of all services, no fileds were dropped, excluded, or modified.
